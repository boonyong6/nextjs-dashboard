import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // experimental: {
  //   ppr: "incremental", // To adopt PPR for specific routes. Only works on the latest canary version.
  // },
};

export default nextConfig;
